import React from 'react';
import { SongButton } from './SongButton';

/* Types */
interface Props {}

/* Global Constants & Functions */

export const VerticalSection: React.FC<Props> = (props) => {
	/* Props Destructure */

	/* State Variables */

	/* Hooks, Queries & Mutations */

	/* Constant, Refs and Memo Constant */

	/* Helper Functions */

	/* Handlers */

	/* Effects */

	/* Renderers */

	return (
		<div className="flex flex-col">
			<SongButton>"Rise by him"</SongButton>
			<SongButton>"Hall of Fame by her"</SongButton>
			<SongButton>"Jobaniyu by me"</SongButton>
			<SongButton>"Fix you by them"</SongButton>
		</div>
	);
};
